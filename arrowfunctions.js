//arrow function o que é
//é uma abstração de uma funçao
//=> operador de arrow 
const multiplica = function(x, y) {
        return x * y;
    }
    //com arrow 
const dividir = (x, y) => {
        return x / y;
    }
    //mais uma contruçõao serve para objetos 
    //deve encapsular com ({})
const objetos = () => ({});